# Bedrock cross-region inference profiles for Claude Haiku 4.5 and Sonnet 4.x.
#
# Real implementation in P0 — T-V2-003.
# Until then this file is intentionally empty; the data sources below are
# placeholders that resolve to known cross-region profiles once enabled.

# data "aws_bedrock_inference_profile" "haiku" {
#   inference_profile_id = var.haiku_inference_profile_id
# }
#
# data "aws_bedrock_inference_profile" "sonnet" {
#   inference_profile_id = var.sonnet_inference_profile_id
# }
